---

openvpn_server__required_packages_names:
  - ca-certificates
  - curl
  - gnupg2

# The name of the openvpn packages
openvpn__packages_names:
  - "openvpn{{ (openvpn_server__version != 'latest')|ternary('='~openvpn_server__version, '') }}"
